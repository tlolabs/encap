# Legacy Python UI references

These captures show the installed Python/PySide6 application before the Rust
engine and native platform clients became primary. They are workflow references,
not pixel-level design requirements.

- [`process-audio.png`](process-audio.png) — episode metadata, output controls,
  artwork, and chapter table empty state.
- [`transcribe.png`](transcribe.png) — local provider, model manager, transcript,
  speaker inspector, and playback empty state.

Captured on macOS from the installed application titled **EnCap Next** during
the 0.2 native rewrite. The corresponding behavior is documented in
[`../legacy-application.md`](../legacy-application.md).
